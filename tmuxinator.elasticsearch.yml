name: es
root: ~/
windows:
  - elasticsearch:
      layout: even-vertical
      panes:
        - "clear && elasticsearch --node.name=spider"
        - "clear && elasticsearch --node.name=spider"
        - "clear && elasticsearch --node.name=spider"
